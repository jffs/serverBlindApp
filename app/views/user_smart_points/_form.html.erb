<%= form_for(@user_smart_point) do |f| %>
  <% if @user_smart_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_smart_point.errors.count, "error") %> prohibited this user_smart_point from being saved:</h2>

      <ul>
      <% @user_smart_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_mac %><br>
    <%= f.text_field :user_mac %>
  </div>
  <div class="field">
    <%= f.label :spoint_mac %><br>
    <%= f.text_field :spoint_mac %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
